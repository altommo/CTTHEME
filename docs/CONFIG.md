# Configuration Guide

This guide explains basic setup options.

1. Enable required plugins.
2. Set `CUSTOMTUBE_URI` and `CUSTOMTUBE_VERSION` constants in `functions.php`.
3. Run `npm install && npm run build` to generate assets.
